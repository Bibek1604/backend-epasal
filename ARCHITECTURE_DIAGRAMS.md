# API Architecture & Flow Diagrams

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    Frontend (React/Vue/Angular)                 │
│                   https://your-domain.com                       │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                    HTTPS + CORS
                   (JWT in header)
                           │
┌──────────────────────────▼──────────────────────────────────────┐
│              Express.js Server (TypeScript)                     │
│          https://backend-epasal.onrender.com                    │
│                                                                  │
│  ┌─────────────────────────────────────────────────────┐       │
│  │ Middleware Stack                                    │       │
│  ├─────────────────────────────────────────────────────┤       │
│  │ 1. Helmet (Security Headers)                        │       │
│  │ 2. CORS (Cross-Origin Protection)                   │       │
│  │ 3. Express.json (Body Parser)                       │       │
│  │ 4. Logger (Development)                             │       │
│  │ 5. Error Handler (Global)                           │       │
│  └─────────────────────────────────────────────────────┘       │
│                                                                  │
│  ┌─────────────────────────────────────────────────────┐       │
│  │ Routes (6 main entities)                            │       │
│  ├─────────────────────────────────────────────────────┤       │
│  │ /products      → ProductController                  │       │
│  │ /categories    → CategoryController                 │       │
│  │ /banners       → BannerController                   │       │
│  │ /orders        → OrderController                    │       │
│  │ /coupons       → CouponController                   │       │
│  │ /flash-sales   → FlashSaleController                │       │
│  └─────────────────────────────────────────────────────┘       │
│                                                                  │
│  ┌─────────────────────────────────────────────────────┐       │
│  │ Controllers (Request Handling)                      │       │
│  │    ↓                                                │       │
│  │ Services (Business Logic)                           │       │
│  │    ↓                                                │       │
│  │ Models (Database Schema)                            │       │
│  │    ↓                                                │       │
│  │ MongoDB (Data Persistence)                          │       │
│  └─────────────────────────────────────────────────────┘       │
│                                                                  │
│  ┌─────────────────────────────────────────────────────┐       │
│  │ Middleware                                          │       │
│  ├─────────────────────────────────────────────────────┤       │
│  │ • authMiddleware (JWT verification)                 │       │
│  │ • validateRequest (Joi schema validation)           │       │
│  │ • errorHandler (Exception handling)                 │       │
│  │ • upload (File upload to Cloudinary)                │       │
│  │ • asyncHandler (Try-catch wrapper)                  │       │
│  └─────────────────────────────────────────────────────┘       │
│                                                                  │
│  ┌─────────────────────────────────────────────────────┐       │
│  │ External Services                                   │       │
│  ├─────────────────────────────────────────────────────┤       │
│  │ • Cloudinary (Image storage & delivery)             │       │
│  │ • MongoDB Atlas (Database)                          │       │
│  │ • JWT (Authentication)                              │       │
│  └─────────────────────────────────────────────────────┘       │
└──────────────────────────┬──────────────────────────────────────┘
                           │
        ┌──────────────────┴──────────────────┐
        │                                     │
        ▼                                     ▼
    Cloudinary                         MongoDB Atlas
    (Image Storage)                     (Database)
```

---

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    Frontend (React)                             │
└──────────────────┬──────────────────────────────────────────────┘
                   │
                   │ 1. User fills login form
                   │
┌──────────────────▼──────────────────────────────────────────────┐
│         Admin Login Endpoint                                    │
│         POST /auth/login (if implemented)                       │
│                                                                  │
│  Input: { email, password }                                     │
│  Process:                                                       │
│  ├─ Verify credentials                                          │
│  ├─ Check role = 'admin'                                        │
│  └─ Generate JWT with JWT_ADMIN_SECRET                          │
│                                                                  │
│  Output: { token: "eyJhbGciOiJIUzI1NiIs..." }                   │
└──────────────────┬──────────────────────────────────────────────┘
                   │
                   │ 2. Store token in localStorage
                   │
                   ▼ 3. Include in Authorization header
       Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
                   │
┌──────────────────▼──────────────────────────────────────────────┐
│         Protected Endpoint                                      │
│         POST /products (requires admin auth)                    │
└──────────────────┬──────────────────────────────────────────────┘
                   │
                   │ 4. Backend authMiddleware processes request
                   │
┌──────────────────▼──────────────────────────────────────────────┐
│  authMiddleware (Extract & Verify Token)                        │
│                                                                  │
│  Steps:                                                         │
│  ├─ Extract "Bearer {token}" from Authorization header         │
│  │                                                              │
│  ├─ Verify JWT signature using JWT_ADMIN_SECRET                │
│  │  └─ If invalid signature → 401 Unauthorized                 │
│  │                                                              │
│  ├─ Check token expiration                                      │
│  │  └─ If expired → 401 Unauthorized                           │
│  │                                                              │
│  ├─ Decode token payload                                        │
│  │  └─ Extract: { id, email, role }                            │
│  │                                                              │
│  ├─ Attach to request: req.user = { id, email, role }         │
│  │                                                              │
│  └─ For admin routes: Check req.user.role === 'admin'         │
│     └─ If not admin → 403 Forbidden                           │
│                                                                  │
│  Result: req.user available in controller                      │
└──────────────────┬──────────────────────────────────────────────┘
                   │
                   ├─ Valid admin? ───→ Proceed to controller
                   │
                   └─ Invalid? ──→ Return error response
                                  {
                                    success: false,
                                    message: "Admin access required"
                                  }
```

---

## 📊 Request-Response Cycle

### Public Endpoint Example: GET /products

```
FRONTEND                              BACKEND
   │                                     │
   │  GET /products?page=1&limit=10     │
   ├────────────────────────────────────►│
   │                                     │
   │                              validateRequest
   │                              (validate query)
   │                                     │
   │                              productController
   │                              .getProducts()
   │                                     │
   │                              productService
   │                              .getAllProducts()
   │                                     │
   │                              MongoDB Query
   │                              db.find({isActive: true})
   │                              .limit(10)
   │                              .skip(0)
   │                                     │
   │◄──────────── Response ──────────────┤
   │ {                                   │
   │   "success": true,                  │
   │   "message": "...",                 │
   │   "data": [ Product[] ],            │
   │   "pagination": {                   │
   │     "page": 1,                      │
   │     "limit": 10,                    │
   │     "total": 150,                   │
   │     "pages": 15                     │
   │   }                                 │
   │ }                                   │
   │                                     │
Process & Render                        (200 OK)
```

### Admin Endpoint Example: POST /products (with auth)

```
FRONTEND                              BACKEND
   │                                     │
   │ POST /products                      │
   │ Authorization: Bearer {token}       │
   │ Content-Type: multipart/form-data   │
   ├────────────────────────────────────►│
   │ Form Fields:                        │
   │ ├─ name                             │
   │ ├─ description                      │
   │ ├─ beforePrice                      │
   │ ├─ ...other fields                  │
   │ └─ image (file)                     │
   │                                     │
   │                              authMiddleware
   │                              (Verify JWT)
   │                                     │
   │                              validateRequest
   │                              (Validate input)
   │                                     │
   │                              upload middleware
   │                              (Upload to Cloudinary)
   │                              Returns: imageUrl
   │                                     │
   │                              productController
   │                              .createProduct()
   │                                     │
   │                              productService
   │                              .createProduct()
   │                                     │
   │                              Generate ID
   │                              Save to MongoDB
   │                                     │
   │◄──────── Response ────────────────────┤
   │ {                                   │
   │   "success": true,                  │
   │   "message": "Product created",     │
   │   "data": {                         │
   │     "id": "prod_xyz",               │
   │     "name": "...",                  │
   │     "imageUrl": "https://...",      │
   │     "created_at": "2024-01-15..."   │
   │   }                                 │
   │ }                                   │
   │                                     │
Update State                           (201 Created)
```

---

## 🔄 Database Relationships

```
                    Products
                        │
        ┌───────────────┼───────────────┐
        │               │               │
        ▼               ▼               ▼
   Categories      Banners         FlashSales
        │                                │
        │                                │
        └────────────┬────────────────────┘
                     │
                     ▼
                   Orders
                     │
        ┌────────────┴────────────┐
        │                         │
        ▼                         ▼
    OrderItems            Coupons (via validation)
    (embedded)            (applied at checkout)


Database Schema Structure:
========================

Product {
  _id: ObjectId (MongoDB)
  id: String (Unique)
  name: String
  category_id: String (Reference to Category)
  ...
  Indexes:
  - { isActive: 1, created_at: -1 }
  - { category_id: 1, isActive: 1 }
  - { name: 'text', description: 'text' }
}

Order {
  _id: ObjectId
  id: String (Unique)
  items: [OrderItem] (Embedded - not referenced)
  user_id?: String (Optional reference)
  status: String
  ...
  Indexes:
  - { status: 1, created_at: -1 }
  - { user_id: 1, status: 1 }
}

FlashSale {
  _id: ObjectId
  id: String (Unique)
  productId: String (Reference)
  startTime, endTime
  isActive: Boolean
  ...
  Indexes:
  - { isActive: 1, startTime: 1, endTime: 1 }
  - { productId: 1, isActive: 1 }
}
```

---

## 🔑 Security Layers Visualization

```
┌─────────────────────────────────────────────────────────────┐
│                  HTTPS Encryption (TLS)                    │
│        All traffic encrypted in transit                    │
└────────────────┬──────────────────────────────────────────┘
                 │
┌────────────────▼──────────────────────────────────────────┐
│                  CORS Validation                          │
│    Check if frontend domain is whitelisted               │
└────────────────┬──────────────────────────────────────────┘
                 │
┌────────────────▼──────────────────────────────────────────┐
│              Helmet Security Headers                      │
│  • X-Frame-Options: DENY                                 │
│  • X-Content-Type-Options: nosniff                       │
│  • X-XSS-Protection: 1; mode=block                       │
│  • Content-Security-Policy: ...                          │
└────────────────┬──────────────────────────────────────────┘
                 │
┌────────────────▼──────────────────────────────────────────┐
│              Request Authentication                       │
│  • Extract JWT from Authorization header                 │
│  • Verify signature using secret key                     │
│  • Check expiration date                                 │
│  • Extract user info from token                          │
└────────────────┬──────────────────────────────────────────┘
                 │
┌────────────────▼──────────────────────────────────────────┐
│         Role-Based Authorization                         │
│  • Public: Anyone                                        │
│  • User: Authenticated (any role)                        │
│  • Admin: Must have role === 'admin'                     │
└────────────────┬──────────────────────────────────────────┘
                 │
┌────────────────▼──────────────────────────────────────────┐
│         Input Validation (Joi Schema)                    │
│  • Type checking                                         │
│  • String length validation                              │
│  • Number range validation                               │
│  • Required field checking                               │
│  • Custom business rules                                 │
└────────────────┬──────────────────────────────────────────┘
                 │
┌────────────────▼──────────────────────────────────────────┐
│         Database Layer Security                          │
│  • MongoDB password authentication                       │
│  • IP whitelist (0.0.0.0/0 for Render)                   │
│  • Schema validation                                     │
│  • Type checking                                         │
│  • Min/Max constraints                                   │
└────────────────┬──────────────────────────────────────────┘
                 │
            ✅ Request Allowed
```

---

## 🌊 Data Flow for Order Creation

```
Customer                Frontend              Backend          Database
   │                       │                    │                │
   │                       │                    │                │
   │ View Products    ────►│                    │                │
   │                       │  GET /products    │                │
   │                       ├───────────────────►│                │
   │                       │                    │ Query Products │
   │                       │                    ├───────────────►│
   │                       │                    │  [Products]    │
   │                       │                    │◄───────────────┤
   │                       │◄───────────────────┤                │
   │   Display Products    │                    │                │
   │◄──────────────────────┤                    │                │
   │                       │                    │                │
   │ Add to Cart           │                    │                │
   │─────────────────►     │                    │                │
   │ (Local state)         │                    │                │
   │                       │                    │                │
   │ View Cart             │                    │                │
   │────────────────────►  │                    │                │
   │ (Review items)        │                    │                │
   │                       │                    │                │
   │ Enter Shipping Info   │                    │                │
   │────────────────────►  │                    │                │
   │ (Name, address, etc)  │                    │                │
   │                       │                    │                │
   │ Apply Coupon          │                    │                │
   │────────────────────►  │  POST /coupons/validate            │
   │                       ├───────────────────►│                │
   │                       │                    │ Validate Code  │
   │                       │                    ├───────────────►│
   │                       │                    │ [Coupon Data]  │
   │                       │                    │◄───────────────┤
   │                       │◄───────────────────┤                │
   │ Show Discount         │                    │                │
   │◄──────────────────────┤                    │                │
   │                       │                    │                │
   │ Click "Place Order"   │                    │                │
   │────────────────────►  │                    │                │
   │                       │  POST /orders      │                │
   │                       │  {                 │                │
   │                       │   name, phone,     │                │
   │                       │   address, items,  │                │
   │                       │   totalAmount      │                │
   │                       │  }                 │                │
   │                       ├───────────────────►│                │
   │                       │                    │ Validate Input │
   │                       │                    │ Generate ID    │
   │                       │                    │ Save to DB     │
   │                       │                    ├───────────────►│
   │                       │                    │ [Order Saved]  │
   │                       │                    │◄───────────────┤
   │                       │                    │                │
   │                       │ {                  │                │
   │                       │  success: true,    │                │
   │                       │  data: {           │                │
   │                       │   id: "order_123", │                │
   │                       │   status: pending  │                │
   │                       │  }                 │                │
   │                       │ }                  │                │
   │                       │◄───────────────────┤                │
   │ Order Confirmation    │                    │                │
   │◄──────────────────────┤                    │                │
   │ Show Order ID         │                    │                │
   │ & Details            │                    │                │
```

---

## 📈 Scaling Considerations

```
Current Architecture (MVP)
════════════════════════════════════════════════════════════
┌─────────────────────────────────────────────────────────┐
│           Single Node.js Server (Render Free)           │
│                                                         │
│         Handles: ~1000 concurrent users                │
│         Database: MongoDB Atlas (512MB free)           │
│         Images: Cloudinary (25GB free)                 │
│                                                         │
│  Limitations:                                           │
│  • Spins down after 15 minutes inactivity              │
│  • Limited database storage                            │
│  • No load balancing                                   │
│  • No caching layer                                    │
└─────────────────────────────────────────────────────────┘

Scaling for Production
════════════════════════════════════════════════════════════
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  ┌────────────────────────────────────────────────┐   │
│  │         Load Balancer (Cloud CDN)              │   │
│  └─────────────────┬──────────────────────────────┘   │
│                    │                                  │
│    ┌───────────────┼───────────────┐                 │
│    │               │               │                 │
│    ▼               ▼               ▼                 │
│  Server 1      Server 2       Server 3              │
│  (Node.js)     (Node.js)      (Node.js)             │
│    │               │               │                 │
│    └───────────────┼───────────────┘                 │
│                    │                                  │
│    ┌───────────────┴───────────────┐                 │
│    │                               │                 │
│    ▼                               ▼                 │
│  Redis Cache               MongoDB Cluster           │
│  (Session Store)           (Sharded)                 │
│                                                         │
│  Additional:                                          │
│  • CloudFlare (CDN/DDoS protection)                  │
│  • S3 (Image backup)                                 │
│  • CloudWatch (Monitoring)                           │
│  • Auto-scaling groups                               │
│  • Database read replicas                            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 🎯 API Response Time Architecture

```
Request Timeline
════════════════════════════════════════════════════════════

Public GET Request (Fast Path)
─────────────────────────────────────────────────────
Time    Component           Action
────────────────────────────────────────────────────
0ms     Network            Request sent
~30ms   CORS Validation    Check origin
~40ms   Helmet             Security headers
~50ms   Route Handler      Match endpoint
~60ms   Query Builder      Prepare MongoDB query
~100ms  Database           Execute query + index lookup
~150ms  Response Builder   Format JSON
~170ms  Network            Response sent
────────────────────────────────────────────────────
~170ms TOTAL for typical product list


Admin POST Request (Slower - Auth + Upload)
─────────────────────────────────────────────────────
Time    Component           Action
────────────────────────────────────────────────────
0ms     Network            Request sent
~50ms   Auth Middleware    Extract & verify JWT
~60ms   CORS/Helmet        Security checks
~80ms   Body Parser        Parse multipart form
~200ms  File Upload        Upload to Cloudinary
~250ms  Validation         Validate all fields
~300ms  Database Operation Insert into MongoDB
~350ms  Response Builder   Format JSON
~370ms  Network            Response sent
────────────────────────────────────────────────────
~370ms TOTAL for product creation


Performance Optimization (With Caching - Production)
─────────────────────────────────────────────────────
Without Cache: 170ms
With Redis Cache: ~5ms (cache hit)
CDN for images: ~100ms → ~20ms
────────────────────────────────────────────────────
Result: 4-8x faster responses
```

---

## 🛠️ Deployment Pipeline

```
┌─────────────────────────────────┐
│   Developer pushes to GitHub    │
│        git push origin main     │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│   GitHub Webhook Triggered      │
│  (Render receives notification) │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│   Render Starts Build           │
│   ├─ Clone repository           │
│   ├─ Run postinstall hook       │
│   │  └─ npm run build           │
│   ├─ Compile TypeScript         │
│   └─ Create dist/ folder        │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│   Start Application             │
│   node dist/server.js           │
│   ├─ Connect to MongoDB         │
│   ├─ Load environment vars      │
│   ├─ Start on port 5000         │
│   └─ Ready for requests         │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│   Live on Internet              │
│  https://backend-epasal.        │
│  onrender.com                   │
└─────────────────────────────────┘
```

---

**Diagrams Updated:** January 2025  
**Architecture Version:** 1.0 (MVP)  
**Status:** ✅ All systems operational
